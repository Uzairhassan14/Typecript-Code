export {}
//Q : 3
let uzair = "hamza";
console.log(uzair.toLocaleLowerCase());
console.log(uzair.toUpperCase());
let first = uzair.charAt(0);
let rem = uzair.slice(1);
let title = first.toUpperCase() + rem;
console.log(`${title}`);